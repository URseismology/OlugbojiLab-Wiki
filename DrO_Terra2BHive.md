> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# ADAMA Seismic Data Processing System Documentation

## 1. Project Overview

The **ADAMA Seismic Data Processing System** is a Python-based suite of Jupyter notebooks designed for advanced seismic data processing and analysis. The system focuses on cross-correlation functions (CCFs) and phase velocity information, leveraging the Obspy library to manage seismic traces. It performs operations such as loading MAT-files, calculating distances using GPS coordinates, and saving results in HDF5 format.

### Key Features:
- **Data Loading and Pre-processing**: Load raw seismic data from various sources.
- **Cross-Correlation Analysis**: Calculate time-domain and frequency-domain CCFs between seismic stations.
- **Phase Velocity Generation**: Generate phase velocity data for Rayleigh and Love waves from AKI estimate files.
- **Data Visualization**: Use visualizations to aid in data interpretation.
- **Error Handling and Logging**: Robust error handling, logging of warnings and read errors, and real-time progress updates.

### Intended Audience:
This documentation is designed to onboard new team members who will be working with the ADAMA system. It provides a comprehensive overview of the project's structure, data architecture, code reference, and workflows.

## 2. Data Architecture

```text
DrO_Terra2BHive/
└── 1_ForSiyu/
    ├── Build_ADAMA_ncfs-checkpoint.ipynb
    ├── Build_ADAMA_ncfsback.ipynb
    ├── Build_ADAMA_pvelraw-Copy1.ipynb
    └── Build_ADAMA_pvelraw.ipynb
```

## 3. Code Reference

### Jupyter Notebooks
#### Build_ADAMA_pvelraw.ipynb
This notebook is responsible for generating phase velocity data from raw seismic data. It follows these steps:
1. **Load Raw Data**: Read MAT-files containing seismic traces.
2. **Pre-process Data**: Apply filters and normalization to the seismic data.
3. **Calculate CCFs**: Perform time-domain and frequency-domain cross-correlation analysis between seismic stations.
4. **Generate Phase Velocity Data**: Use AKI estimate files to calculate phase velocities for Rayleigh and Love waves.
5. **Save Results**: Store the phase velocity data in HDF5 format.

#### Build_ADAMA_ncfsback.ipynb
This notebook performs back-processing of cross-correlation functions (CCFs). It follows these steps:
1. **Load CCFs**: Read CCFs from HDF5 files.
2. **Back-Process Data**: Apply inverse operations to recover the original seismic data or perform additional analysis.
3. **Save Results**: Store the processed data in HDF5 format.

#### Build_ADAMA_ncfs-checkpoint.ipynb
This notebook serves as a checkpoint for intermediate processing steps. It follows these steps:
1. **Load Intermediate Data**: Read partially processed data from HDF5 files.
2. **Perform Additional Processing**: Apply any necessary additional processing steps.
3. **Save Results**: Store the updated data in HDF5 format.

### Key Functions and Libraries
- **Obspy Library**: Used for managing seismic traces, including loading, filtering, and saving data.
  - `read(filename)`: Load seismic data from a file.
  - `filter(type, freqmin, freqmax)`: Apply frequency filters to the data.
  - `write(filename, format)`: Save seismic data to a file in the specified format.
- **HDF5 Library**: Used for storing and retrieving processed data.
  - `h5py.File(filename, 'w')`: Create or open an HDF5 file.
  - `dataset.create_dataset(name, data)`: Create a dataset within an HDF5 file.
- **Matplotlib**: Used for data visualization.
  - `plt.plot(x, y)`: Plot data.
  - `plt.show()`: Display the plot.

## 4. Workflows

### Data Flow
1. **Data Ingestion**:
   - Raw seismic data is loaded from MAT-files using Obspy.
   - GPS coordinates and station information are extracted.

2. **Pre-processing**:
   - Seismic traces are filtered to remove noise.
   - Data normalization is applied to ensure consistency.

3. **Cross-Correlation Analysis**:
   - Time-domain cross-correlation functions (CCFs) are calculated between seismic stations.
   - Frequency-domain CCFs are generated using Fourier transforms.

4. **Phase Velocity Calculation**:
   - Phase velocity data for Rayleigh and Love waves is derived from the CCFs using AKI estimate files.
   - Results are stored in HDF5 format with detailed metadata.

5. **Back-Processing**:
   - CCFs are loaded from HDF5 files.
   - Inverse operations are applied to recover the original seismic data or perform additional analysis.
   - Updated data is saved in HDF5 format.

6. **Data Visualization**:
   - Visualizations are generated using Matplotlib to aid in data interpretation.
   - Plots include time-domain and frequency-domain CCFs, phase velocity spectra, and other relevant metrics.

### Example Workflow
1. **Step 1: Load Raw Data**
   ```python
   from obspy import read

   # Load seismic data from a MAT-file
   raw_data = read('path_to_mat_file.mat')
   ```

2. **Step 2: Pre-process Data**
   ```python
   from obspy.signal.filter import bandpass

   # Apply a bandpass filter to the data
   filtered_data = bandpass(raw_data, freqmin=0.1, freqmax=1.0)
   ```

3. **Step 3: Calculate CCFs**
   ```python
   from scipy.signal import correlate

   # Calculate time-domain cross-correlation function
   ccf_time = correlate(filtered_data[0], filtered_data[1])

   # Calculate frequency-domain cross-correlation function
   fft_data_1 = np.fft.fft(filtered_data[0])
   fft_data_2 = np.fft.fft(filtered_data[1])
   ccf_freq = np.fft.ifft(fft_data_1 * np.conj(fft_data_2))
   ```

4. **Step 4: Generate Phase Velocity Data**
   ```python
   import h5py

   # Load AKI estimate file
   aki_estimate = load_aki_estimate('path_to_aki_file.txt')

   # Calculate phase velocity for Rayleigh and Love waves
   phase_velocity_rayleigh = calculate_phase_velocity(ccf_time, aki_estimate, wave_type='rayleigh')
   phase_velocity_love = calculate_phase_velocity(ccf_time, aki_estimate, wave_type='love')

   # Save results to HDF5 file
   with h5py.File('phase_velocity_data.hdf5', 'w') as f:
       f.create_dataset('rayleigh', data=phase_velocity_rayleigh)
       f.create_dataset('love', data=phase_velocity_love)
   ```

5. **Step 5: Back-Processing**
   ```python
   # Load CCFs from HDF5 file
   with h5py.File('ccf_data.hdf5', 'r') as f:
       ccf_time = f['time'][:]
       ccf_freq = f['freq'][:]

   # Apply inverse operations to recover original data
   recovered_data = apply_inverse_operations(ccf_time, ccf_freq)

   # Save results to HDF5 file
   with h5py.File('recovered_data.hdf5', 'w') as f:
       f.create_dataset('recovered', data=recovered_data)
   ```

6. **Step 6: Visualize Data**
   ```python
   import matplotlib.pyplot as plt

   # Plot time-domain CCF
   plt.plot(ccf_time)
   plt.title('Time-Domain Cross-Correlation Function')
   plt.xlabel('Lag (s)')
   plt.ylabel('Amplitude')
   plt.show()

   # Plot phase velocity spectrum for Rayleigh waves
   plt.plot(frequencies, phase_velocity_rayleigh)
   plt.title('Phase Velocity Spectrum (Rayleigh Waves)')
   plt.xlabel('Frequency (Hz)')
   plt.ylabel('Phase Velocity (m/s)')
   plt.show()
   ```

This comprehensive workflow ensures that the ADAMA system efficiently manages and analyzes seismic data for detailed geophysical studies.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `DrO_Terra2BHive_codebase_*.tar.gz`
