> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# BluehiveR HPC Cluster Documentation

Welcome to the official documentation for the BluehiveR High-Performance Computing (HPC) cluster setup project. This guide is designed to onboard new team members by providing a comprehensive overview of the project, its architecture, codebase, and workflows.

## 1. Project Overview

### Purpose
The BluehiveR HPC cluster setup project aims to automate the configuration and management of a high-performance computing environment using MATLAB. The primary goal is to ensure that the cluster can efficiently run complex computational tasks with minimal manual intervention. This project supports multiple operating systems and MATLAB releases, making it versatile for various deployment scenarios.

### Key Components
- **`._cluster_installer.m`:** The core script responsible for automating the entire setup process.
- **`cluster_installer.m`:** Tailored for Windows systems with MATLAB release 9.4.0.813654.
- **`cluster_installer2.m`:** Designed for Unix-based systems.

### Target Audience
This documentation is intended for new team members, system administrators, and developers who will be working on or maintaining the BluehiveR HPC cluster setup.

## 2. Data Architecture

```text
Software/
├── ._cluster_installer.m
├── cluster_installer.m
└── cluster_installer2.m
```

## 3. Code Reference

### `._cluster_installer.m`
This script is the heart of the project, responsible for automating the entire setup process. It handles the following tasks:

1. **Software Installations:**
   - Downloads and installs necessary software packages.
   - Ensures all dependencies are met.

2. **Configurations:**
   - Sets up network configurations.
   - Configures environment variables and system settings.

3. **Dependencies:**
   - Checks for existing installations and updates them if necessary.
   - Installs missing dependencies.

4. **Log Management:**
   - Generates log files to track the setup process.
   - Logs any errors encountered during installation or configuration.

### `cluster_installer.m`
This script is tailored for Windows systems and performs the following tasks:

1. **Cluster Profile Configuration:**
   - Prompts the user to replace existing profiles if they exist.
   - Configures the root directory, job storage path, unique subfolders, submission arguments, and worker node counts.

2. **Profile Creation:**
   - Creates a new cluster profile with the specified settings.
   - Ensures consistency across multiple nodes.

### `cluster_installer2.m`
This script is designed for Unix-based systems and performs similar tasks to `cluster_installer.m`:

1. **Cluster Profile Configuration:**
   - Prompts the user to replace existing profiles if they exist.
   - Configures the root directory, job storage path, unique subfolders, submission arguments, and worker node counts.

2. **Profile Creation:**
   - Creates a new cluster profile with the specified settings.
   - Ensures consistency across multiple nodes.

## 4. Workflows

### Data Flow Through Scripts
The data flow through the scripts is designed to ensure a smooth and efficient setup process:

1. **Initialization:**
   - The user runs `._cluster_installer.m` with the necessary input parameters (administrator credentials, network details, software versions, installation paths).
   - The script initializes the setup process by creating log files and checking for existing installations.

2. **Software Installations:**
   - The script downloads and installs required software packages.
   - It checks for dependencies and installs any missing components.

3. **Configurations:**
   - Network configurations are set up to ensure proper communication between nodes.
   - Environment variables and system settings are configured.

4. **Cluster Profile Configuration:**
   - If the user is running on a Windows system, `cluster_installer.m` is executed.
   - If the user is running on a Unix-based system, `cluster_installer2.m` is executed.
   - Both scripts prompt the user to replace existing profiles if they exist and configure the cluster settings.

5. **Profile Creation:**
   - A new cluster profile is created with the specified settings.
   - The script ensures that all nodes are configured consistently.

6. **Finalization:**
   - Log files are updated with the final status of the setup process.
   - Any errors encountered during the setup are logged for troubleshooting.

### Example Workflow
1. **Step 1:** Run `._cluster_installer.m` with the following input parameters:
   ```matlab
   .cluster_installer('admin_username', 'admin_password', 'network_details', 'software_versions', 'installation_paths');
   ```

2. **Step 2:** The script initializes and checks for existing installations.
3. **Step 3:** Software packages are downloaded and installed, and dependencies are checked.
4. **Step 4:** Network configurations are set up, and environment variables are configured.
5. **Step 5:** If the system is Windows, `cluster_installer.m` is executed:
   ```matlab
   cluster_installer('root_directory', 'job_storage_path', 'unique_subfolders', 'submission_arguments', 'worker_node_counts');
   ```
6. **Step 6:** The script prompts the user to replace existing profiles and configures the cluster settings.
7. **Step 7:** A new cluster profile is created, ensuring consistency across all nodes.
8. **Step 8:** Log files are updated with the final status of the setup process.

By following this workflow, new team members can efficiently set up and manage the BluehiveR HPC cluster on both Windows and Unix-based systems.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Software_codebase_*.tar.gz`
