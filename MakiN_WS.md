> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# ROOT Directory Documentation

## 1. Project Overview

The 'ROOT' directory is designed to facilitate the execution of Python-based Jupyter Notebooks in a Linux environment, ensuring consistent and reproducible results. The primary components of this project include:

- **Environment Setup**: A shell script (`LoadPythonEnv.sh`) that activates an Anaconda environment and launches Jupyter Notebooks.
- **Data Storage**: A directory containing various CSV files for data processing and analysis.
- **Notebook Execution**: A sample Jupyter Notebook (`Helloworld.ipynb`) demonstrating basic data handling using pandas.

This setup is intended to support automated and controlled execution of notebooks, ensuring that all dependencies are managed consistently. The project aims to provide a robust framework for data scientists and researchers to perform their tasks efficiently.

## 2. Data Architecture

### Data Patterns and Structure

The data in this project is organized into CSV files, each representing different datasets. These files are stored in the `/scratch/tolugboj_lab/MakiN_WS` directory. The filenames follow a consistent pattern:

- **Naming Conventions**: Each file name includes network code, station code, location code, and channel code (e.g., `CI.GPO..BHZ.M.csv`).
- **Content**: The CSV files contain time-series data from seismic stations, typically including columns for timestamp, amplitude, and other relevant metadata.

### Data Files

The following are examples of the data files in the project:

- `CI.GPO..BHZ.M.csv`: Contains BHZ channel data from the CI network at the GPO station.
- `IU.KEV..BHZ.M.csv`: Contains BHZ channel data from the IU network at the KEV station.
- `fail_to_download.txt`: A text file listing files that failed to download.

### Data Patterns

- **Network Codes**: Abbreviations for seismic networks (e.g., CI, IU).
- **Station Codes**: Identifiers for individual stations within a network (e.g., GPO, KEV).
- **Location Codes**: Typically empty or '00' to indicate the default location.
- **Channel Codes**: Indicates the type of data channel (e.g., BHZ for vertical component).

## 3. Code Reference

### Environment Setup Script: `LoadPythonEnv.sh`

#### Functionality
The `LoadPythonEnv.sh` script is responsible for setting up the environment and launching Jupyter Notebooks. It performs the following tasks:

1. **Activate Anaconda Environment**: Activates the "instaseis" conda environment.
2. **Navigate to Notebook Directory**: Changes the current directory to `/scratch/tolugboj_lab/MakiN_WS`.
3. **Launch Jupyter Notebooks**: Starts Jupyter Notebooks in a non-interactive mode.

#### Code
```sh
#!/bin/bash

# Activate the Anaconda environment
source /path/to/anaconda3/bin/activate instaseis

# Navigate to the notebook directory
cd /scratch/tolugboj_lab/MakiN_WS

# Launch Jupyter Notebooks in non-interactive mode
jupyter notebook --no-browser --port=8888
```

### Sample Notebook: `Helloworld.ipynb`

#### Functionality
The `Helloworld.ipynb` notebook is designed to demonstrate basic data handling using pandas. It performs the following tasks:

1. **Import Libraries**: Imports necessary libraries, including pandas.
2. **Read CSV File**: Reads a sample CSV file and displays its contents.

#### Code
```python
# Import necessary libraries
import pandas as pd

# Read a sample CSV file
data = pd.read_csv('CI.GPO..BHZ.M.csv')

# Display the first few rows of the data
print(data.head())
```

### Error Handling
The current implementation of `Helloworld.ipynb` contains syntax errors. To correct these issues, ensure that:

- The library imports are correct.
- The file path is valid and accessible.

#### Corrected Code
```python
# Import necessary libraries
import pandas as pd

# Read a sample CSV file
data = pd.read_csv('/scratch/tolugboj_lab/MakiN_WS/CI.GPO..BHZ.M.csv')

# Display the first few rows of the data
print(data.head())
```

## 4. Workflows

### Data Flow Through Scripts

1. **Environment Setup**:
   - The `LoadPythonEnv.sh` script is executed to set up the environment.
   - It activates the "instaseis" conda environment and navigates to the notebook directory.

2. **Notebook Execution**:
   - Jupyter Notebooks are launched in non-interactive mode.
   - Users can open and run notebooks, such as `Helloworld.ipynb`.

3. **Data Processing**:
   - The `Helloworld.ipynb` notebook reads a CSV file from the data directory.
   - It processes the data using pandas and displays the results.

### Example Workflow

1. **Run Environment Setup Script**:
   ```sh
   ./LoadPythonEnv.sh
   ```

2. **Open Jupyter Notebook**:
   - Open a web browser and navigate to `http://localhost:8888`.
   - Locate and open `Helloworld.ipynb`.

3. **Execute Notebook Cells**:
   - Run the cells in `Helloworld.ipynb` to read and display the data from the CSV file.

### Diagram

```plaintext
+-------------------+
|  LoadPythonEnv.sh |
+-------------------+
        |
        v
+-------------------+
| Activate Environment|
| Navigate to Directory|
| Launch Jupyter Notebooks|
+-------------------+
        |
        v
+-------------------+
|   Helloworld.ipynb   |
+-------------------+
        |
        v
+-------------------+
|  Read CSV File      |
|  Process Data       |
|  Display Results    |
+-------------------+
```

This workflow ensures that the environment is consistently set up, and notebooks can be executed reliably to process and analyze data.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `MakiN_WS_codebase_*.tar.gz`
