> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
### Seismic Data Processing and Analysis System for USARRAY Earthscope

#### 1. Project Overview

The **Seismic Data Processing and Analysis System for USARRAY Earthscope** is a comprehensive environment designed to handle seismic data from the USARRAY project, which is part of the EarthScope initiative. This system integrates various tools and scripts written in MATLAB and Python to automate tasks such as data downloading, preprocessing, advanced signal processing, and visualization. The primary goals of this project include:

- **Data Management**: Efficiently manage and organize large volumes of seismic data.
- **Data Preprocessing**: Automate the cleaning and preparation of raw seismic data for analysis.
- **Signal Processing**: Apply advanced algorithms to extract meaningful information from seismic signals.
- **Visualization**: Generate high-quality visualizations to aid in the interpretation of seismic data.

The system is organized into several sub-modules, each serving specific roles in the seismic data workflow. These modules are designed to work together seamlessly, ensuring a robust and reproducible data processing pipeline.

#### 2. Data Architecture

The data architecture of the project is designed to handle large volumes of seismic data efficiently. The data is organized into structured directories, with each directory containing specific types of files. Here's an overview of the data patterns:

- **Root Directory (ROOT)**: Contains the main scripts and configuration files that orchestrate the entire workflow.
- **Data Directories**: Each sub-module has its own data directory to store raw and processed data.

##### Data Directory Structure

```
ROOT
│
├── 0_RFImager_Upload
│   ├── 2_Data
│   │   └── Waveforms
│   │       └── Parallel_Download
│   │           ├── SAC
│   │           │   ├── US
│   │           │   │   ├── M1 (Station Data)
│   │           │   │   ├── M2 (Station Data)
│   │           │   │   └── ...
│   │           │   └── CN
│   │           │       ├── INK (Station Data)
│   │           │       └── WHY (Station Data)
│   │           └── EqData.mat (Event Data)
│   └── alreadyHaveSAC.mat (Tracking File)
│
├── 2_Data
│   └── Waveforms
│       └── Parallel_Download
│           ├── SAC
│           │   ├── US
│           │   │   ├── M1 (Station Data)
│           │   │   ├── M2 (Station Data)
│           │   │   └── ...
│           │   └── CN
│           │       ├── INK (Station Data)
│           │       └── WHY (Station Data)
│           └── EqData.mat (Event Data)
│
├── 3_src
│   ├── EventDwnld
│   │   └── sample
│   │       └── obspy_batch_0318
│   │           ├── AK
│   │           │   └── AKM1 (Station Data)
│   │           └── CI
│   │               └── VTV
│   │                   └── CI-VTV-2021-07-21T14:26:00.613000Z
│   │                       ├── data
│   │                       ├── logs
│   │                       └── metadata
│
├── 4_BinDrO
│   ├── bin
│   │   └── dro (Executable)
│   └── src
│       └── main.cpp (Source Code)
│
├── Prj2_SEUS_RF
│   ├── data
│   │   └── PsReceiverFunctions.mat
│   ├── scripts
│   │   └── process_rf.m
│   └── results
│       └── plots
│
├── RFImager_EvansVersion
│   ├── data
│   │   └── seismic_data.mat
│   ├── scripts
│   │   └── analyze_data.m
│   └── results
│       └── visualizations
└── Simulation_Template_Folder
    ├── config
    │   └── simulation_params.json
    ├── data
    │   └── input_data.nc
    └── output
        └── simulation_results.nc
```

### 3. Code Reference

The project's codebase is organized into several modules, each containing scripts and functions that perform specific tasks. Here’s a detailed breakdown of the key components:

#### **ROOT Directory**

- **Main Scripts**: `main.m` (MATLAB), `main.py` (Python)
  - These scripts serve as entry points for the entire workflow. They coordinate data processing by calling other scripts and functions.

#### **0_RFImager_Upload Module**

- **Data Management**:
  - `manage_data.m`: Manages the organization of raw and processed seismic data.
- **Preprocessing**:
  - `preprocess_data.m`: Cleans and prepares raw seismic data for analysis.
- **Visualization**:
  - `visualize_data.m`: Generates visualizations to aid in data interpretation.

#### **2_Data Module**

- **Data Downloading**:
  - `download_data.py`: Downloads seismic data from external sources (e.g., IRIS).
- **Data Conversion**:
  - `convert_to_sac.py`: Converts raw data into SAC format for further processing.

#### **3_src Module**

- **Event Downloading**:
  - `event_download.py`: Downloads event data from the USGS Earthquake Catalog.
- **Data Organization**:
  - `organize_data.sh`: Shell script to organize downloaded data into a structured directory.

#### **4_BinDrO Module**

- **Signal Processing**:
  - `dro.cpp`: C++ source code for the DRO (Discrete Radon Operator) algorithm, compiled into an executable.
- **Data Analysis**:
  - `analyze_data.m`: MATLAB script to analyze processed seismic data using the DRO algorithm.

#### **Prj2_SEUS_RF Module**

- **Receiver Function Analysis**:
  - `process_rf.m`: MATLAB script to process Ps receiver functions and detect upper mantle discontinuities.
- **Data Visualization**:
  - `plot_results.m`: Generates visualizations of the results from receiver function analysis.

#### **RFImager_EvansVersion Module**

- **Seismic Data Analysis**:
  - `analyze_data.m`: MATLAB script to perform advanced seismic data analysis using custom functions and external tools like TauP.
- **Visualization**:
  - `visualize_results.m`: Generates high-quality visualizations of the analysis results.

#### **Simulation_Template_Folder Module**

- **Configuration**:
  - `simulation_params.json`: JSON file containing parameters for the simulation.
- **Data Input**:
  - `input_data.nc`: NetCDF file containing input data for the simulation.
- **Data Output**:
  - `simulation_results.nc`: NetCDF file containing the results of the simulation.

### 4. Workflows

The workflows in this project describe how data flows through the scripts and functions, from raw data acquisition to advanced analysis and visualization. Here’s a step-by-step overview of the key workflows:

#### **Data Acquisition Workflow**

1. **Download Raw Data**:
   - `download_data.py` is used to download seismic data from external sources (e.g., IRIS) and store it in the `2_Data/Waveforms/Parallel_Download/SAC` directory.
2. **Convert Data to SAC Format**:
   - `convert_to_sac.py` converts the downloaded raw data into SAC format, which is a standard format for seismic data processing.

#### **Data Preprocessing Workflow**

1. **Organize Data**:
   - `organize_data.sh` organizes the downloaded data into a structured directory, making it easier to manage and process.
2. **Preprocess Data**:
   - `preprocess_data.m` cleans and prepares the raw seismic data for analysis, including removing noise and normalizing the signals.

#### **Seismic Event Download Workflow**

1. **Download Event Data**:
   - `event_download.py` downloads event data from the USGS Earthquake Catalog and stores it in the `3_src/EventDwnld/sample/obspy_batch_0318` directory.
2. **Organize Event Data**:
   - The downloaded event data is organized into a structured format, making it ready for further analysis.

#### **Advanced Signal Processing Workflow**

1. **Apply DRO Algorithm**:
   - `dro.cpp` compiles and runs the Discrete Radon Operator (DRO) algorithm to process seismic data.
2. **Analyze Processed Data**:
   - `analyze_data.m` uses the processed data from the DRO algorithm to perform advanced signal processing and analysis.

#### **Receiver Function Analysis Workflow**

1. **Process Ps Receiver Functions**:
   - `process_rf.m` processes Ps receiver functions to detect upper mantle discontinuities.
2. **Visualize Results**:
   - `plot_results.m` generates visualizations of the results from the receiver function analysis, providing insights into the Earth's crust and upper mantle.

#### **Simulation Workflow**

1. **Configure Simulation Parameters**:
   - `simulation_params.json` is used to configure the parameters for the seismic wave propagation simulation.
2. **Run Simulation**:
   - The simulation is run using the AxiSEM3D tool, with input data provided in `input_data.nc`.
3. **Analyze Simulation Results**:
   - `analyze_simulation_results.m` processes the results of the simulation and generates visualizations to aid in interpretation.

### Conclusion

The Seismic Data Processing and Analysis System for USARRAY Earthscope is a robust and modular environment designed to handle large volumes of seismic data efficiently. By integrating various tools and scripts, it automates the entire workflow from data acquisition to advanced analysis and visualization. The structured directory layout and well-defined workflows ensure that the system is both scalable and reproducible, making it an invaluable resource for researchers and practitioners in the field of seismology.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj7_RadonT_codebase_*.tar.gz`
