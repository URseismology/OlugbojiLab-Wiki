> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# Root Subsystem: Seismic and Genomic Data Management Framework

## 1. Project Overview

The root subsystem is a comprehensive framework designed to manage and process seismic and genomic data, with a specific focus on network GE and station BOAB in seismology, as well as North American genomic datasets. The system is modular and highly scalable, ensuring efficient data handling and processing within an HPC (High-Performance Computing) environment.

### Key Features:
- **Automated Data Transformation**: Converts seismic station data from CSV to text format.
- **Advanced Data Management**: Pre-processing, standardization, and extraction of genomic information.
- **Robust Preprocessing Workflows**: Automated downloading, discrepancy identification, and core processing tasks for seismic data.
- **Efficient Data Storage**: Consolidates processed data into HDF5 files with comprehensive logging.

## 2. Data Architecture

```text
Prj_terraSeis/
├── input/
│   └── trans_cat_txt.sh
└── source/
    ├── Updated_merge_daily_to_monthly_single.py
    ├── batch_processing_script_LH_prioritized.sh
    ├── batch_processing_script_LH_prioritized_adaptive.slurm
    ├── buildASDF.py
    ├── data_packaging.py
    ├── generate_stations_list.ipynb
    ├── generate_stations_list.py
    ├── generate_stations_list.slurm
    ├── job_merge_month.sh
    ├── org_siyu/
    │   ├── cancel_jobs.sh
    │   ├── generate_diff.py
    │   ├── ppToHDF5.py
    │   ├── pre_pross.py
    │   ├── pre_pross.sh
    │   ├── pre_pross.slurm
    │   └── setup.py
    ├── post_pross.py
    ├── post_pross.sh
    ├── post_pross.slurm
    ├── process_gml_script_LH.py
    ├── process_gml_script_LH_single.py
    ├── rover_download_all.sh
    ├── rover_download_test10.slurm
    ├── test_10.sh
    ├── test_ASDF.slurm
    └── test_NorthAmerica.sh
```

## 3. Code Reference

### Input Module
The `input` module is responsible for transforming seismic station data from CSV to text format.

#### Scripts:
- **trans_cat_txt.sh**: Converts CSV files to a consistent text format.
  ```sh
  # Example usage
  ./trans_cat_txt.sh input/station_data.csv output/station_data.txt
  ```

### Source Module
The `source` module provides advanced data management and processing scripts.

#### Scripts:
- **post_pross.py**: Post-processing script for seismic data.
  ```python
  # Example usage
  python post_pross.py --input_path input/processed_data --output_path output/final_data
  ```

- **buildASDF.py**: Standardizes seismic data formats.
  ```python
  # Example usage
  python buildASDF.py --data_path input/seismic_data --output_file output/seismic.asdf
  ```

- **process_gml_script_LH.py**: Extracts genomic information from North American datasets.
  ```python
  # Example usage
  python process_gml_script_LH.py --input_path input/genomic_data --output_path output/processed_genomic_data
  ```

### Org Siyu Sub-Module
The `org_siyu` sub-module automates the preprocessing of seismic data.

#### Scripts:
- **setup.py**: Downloads inventory files for multiple stations.
  ```python
  # Example usage
  python setup.py --station_list input/station_list.txt --output_dir output/inventory_files
  ```

- **generate_diff.py**: Identifies discrepancies between raw and processed data.
  ```python
  # Example usage
  python generate_diff.py --raw_data_path input/raw_data --processed_data_path output/processed_data --output_file output/discrepancies.txt
  ```

- **pre_pross.slurm**: Orchestrates core processing tasks (trace merging, channel selection, response removal, downsampling, and saving in SAC format).
  ```sh
  # Example usage
  sbatch pre_pross.slurm --input_path input/processed_data --output_path output/final_data
  ```

- **ppToHDF5.py**: Consolidates processed data into HDF5 files.
  ```python
  # Example usage
  python ppToHDF5.py --data_path input/final_data --output_file output/data.hdf5
  ```

## 4. Workflows

### Seismic Data Workflow

1. **Data Input**:
   - Raw seismic station data is stored in the `input/` directory in CSV format.
   ```sh
   # Example: input/station_data.csv
   ```

2. **Data Transformation**:
   - The `trans_cat_txt.sh` script converts CSV files to a consistent text format.
   ```sh
   ./trans_cat_txt.sh input/station_data.csv output/station_data.txt
   ```

3. **Inventory Download**:
   - The `setup.py` script downloads inventory files for multiple stations.
   ```python
   python setup.py --station_list input/station_list.txt --output_dir output/inventory_files
   ```

4. **Data Preprocessing**:
   - The `pre_pross.slurm` script orchestrates core processing tasks (trace merging, channel selection, response removal, downsampling, and saving in SAC format).
   ```sh
   sbatch pre_pross.slurm --input_path input/processed_data --output_path output/final_data
   ```

5. **Discrepancy Identification**:
   - The `generate_diff.py` script identifies discrepancies between raw and processed data.
   ```python
   python generate_diff.py --raw_data_path input/raw_data --processed_data_path output/processed_data --output_file output/discrepancies.txt
   ```

6. **Data Consolidation**:
   - The `ppToHDF5.py` script consolidates processed data into HDF5 files.
   ```python
   python ppToHDF5.py --data_path input/final_data --output_file output/data.hdf5
   ```

### Genomic Data Workflow

1. **Data Input**:
   - Raw genomic datasets from North American sources are stored in the `input/` directory.
   ```sh
   # Example: input/genomic_data/
   ```

2. **Data Processing**:
   - The `process_gml_script_LH.py` script extracts and standardizes genomic information.
   ```python
   python process_gml_script_LH.py --input_path input/genomic_data --output_path output/processed_genomic_data
   ```

3. **Data Consolidation**:
   - The processed genomic data is consolidated into HDF5 files using `ppToHDF5.py`.
   ```python
   python ppToHDF5.py --data_path input/processed_genomic_data --output_file output/genomic_data.hdf5
   ```

### Data Flow Diagram

```
[Input Module]
  +-------------------+
  | trans_cat_txt.sh  |
  +-------------------+
           |
           v
[Source Module]
  +---------------------+    +---------------------+    +----------------------+
  | post_pross.py       | -> | buildASDF.py        | -> | process_gml_script_LH|
  +---------------------+    +---------------------+    +----------------------+
           |                        |
           v                        v
[Org Siyu Sub-Module]
  +-------------------+    +------------------+    +--------------------+    +------------------+
  | setup.py          | -> | generate_diff.py | -> | pre_pross.slurm    | -> | ppToHDF5.py      |
  +-------------------+    +------------------+    +--------------------+    +------------------+
           |                        |                        |                        |
           v                        v                        v                        v
[Output]
  +---------------------+    +----------------------+    +----------------------+
  | output/final_data   |    | output/data.hdf5     |    | output/genomic_data.hdf5|
  +---------------------+    +----------------------+    +----------------------+
```

This modular and well-structured approach ensures that the root subsystem can efficiently manage and process large volumes of seismic and genomic data, supporting both research and operational workflows.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj_terraSeis_codebase_*.tar.gz`
