> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# Global Earthquake Study Documentation

## 1. Project Overview

The **Global Earthquake Study** project is a comprehensive initiative aimed at analyzing seismic data from various global locations. The project focuses on efficient data management, ensuring that all relevant seismic data is accurately stored, backed up, and available for research activities. The `Data/test_data_only/Not_Rotated` subsystem is a critical component of this project, responsible for handling the migration and synchronization of seismic data.

### Key Components:
- **SAC Module**: Automates data transfer using a Bash script.
- **YKA Module**: Ensures comprehensive data backup and synchronization using a shell script.

## 2. Data Architecture

The data architecture is designed to organize and manage seismic data from multiple stations over different years. The primary directories are structured as follows:

### Directory Structure
- **Data/test_data_only/Not_Rotated**:
  - Contains raw, non-rotated seismic data.
  - Subdirectories for each station (e.g., YKA/YKW1) and year (e.g., 2008).

- **Data/test_data_only/Rotated**:
  - Contains rotated seismic data.
  - Similar subdirectory structure as `Not_Rotated`.

### Data Patterns
- **File Naming Conventions**:
  - Files are named using a standardized format: `YKA.YKW*.*.*.*.*.XXX`, where `XXX` represents the channel (e.g., R, T, Z).
  - Additional channels like BHE, BHN, and BHZ may be present in some directories.
  - Some files have extensions like `.sac` or `.txt`.

- **Data Types**:
  - Raw seismic data (SAC format).
  - Rotated seismic data (R, T, Z channels).
  - Backup and transfer scripts (Bash and shell scripts).

### Example Directory
```
Data/test_data_only/Not_Rotated/YKA/YKW1/2008/
    YKA.YKW*.*.*.*.*.BHN.sac (16 files, 0.52 MB)
    YKA.YKW*.*.*.*.*.BHZ.sac (16 files, 0.52 MB)
    YKA.YKW*.*.*.*.*.BHE.sac (16 files, 0.52 MB)
```

## 3. Code Reference

### SAC Module
The `SAC` module is responsible for automating the data transfer process using a Bash script.

#### Script: `tmp_copy.sh`
- **Location**: `Data/test_data_only/Not_Rotated/SAC/YKA/YKAW1/`
- **Functionality**:
  - Scans and silently moves files from the source directory to the destination directory.
  - Source Directory: `/scratch/tolugboj_lab/Global_Eq_Study/Data/YKA/YKAW1/`
  - Destination Directory: `/scratch/tolugboj_lab/Global_Eq_Study/Data/SAC/YKAW1/`

#### Example Usage
```bash
#!/bin/bash

# Define source and destination directories
SOURCE_DIR="/scratch/tolugboj_lab/Global_Eq_Study/Data/YKA/YKAW1/"
DEST_DIR="/scratch/tolugboj_lab/Global_Eq_Study/Data/SAC/YKAW1/"

# Move files silently
mv -f "$SOURCE_DIR"*.sac "$DEST_DIR"
```

### YKA Module
The `YKA` module ensures comprehensive data backup and synchronization using a shell script.

#### Script: `tmp_copy.sh`
- **Location**: `Data/test_data_only/Not_Rotated/YKA/YKW1/`
- **Functionality**:
  - Recursively copies all files and directories from the source directory to the destination directory.
  - Source Directory: `/scratch/tolugboj_lab/Global_Eq_Study/Data/YKA/YKAW1/`
  - Destination Directory: Same as `SAC` module's destination

#### Example Usage
```bash
#!/bin/bash

# Define source and destination directories
SOURCE_DIR="/scratch/tolugboj_lab/Global_Eq_Study/Data/YKA/YKAW1/"
DEST_DIR="/scratch/tolugboj_lab/Global_Eq_Study/Data/SAC/YKAW1/"

# Recursively copy files and directories
cp -r "$SOURCE_DIR" "$DEST_DIR"
```

## 4. Workflows

### Data Flow Through Scripts

#### Step-by-Step Workflow

1. **Data Collection**:
   - Seismic data is collected from various stations (e.g., YKA/YKW1) and stored in the `Not_Rotated` directory.

2. **Data Transfer (SAC Module)**:
   - The `tmp_copy.sh` script in the `SAC` module scans the source directory (`/scratch/tolugboj_lab/Global_Eq_Study/Data/YKA/YKAW1/`).
   - It moves the `.sac` files to the destination directory (`/scratch/tolugboj_lab/Global_Eq_Study/Data/SAC/YKAW1/`).

3. **Data Backup (YKA Module)**:
   - The `tmp_copy.sh` script in the `YKA` module recursively copies all files and directories from the source directory to the destination directory.
   - This ensures that a backup of the data is maintained.

4. **Data Rotation**:
   - Rotated seismic data is stored in the `Rotated` directory, following a similar structure as the `Not_Rotated` directory.

5. **Data Analysis**:
   - Researchers can access the synchronized and backed-up data for analysis from the destination directories.

### Diagram
```
+---------------------------------+
| Data Collection (YKA/YKW1)      |
+---------------------------------+
                |
                v
+---------------------------------+
| Data Transfer (SAC Module)       |
| tmp_copy.sh                     |
| - Moves .sac files              |
+---------------------------------+
                |
                v
+---------------------------------+
| Data Backup (YKA Module)         |
| tmp_copy.sh                     |
| - Recursively copies all data   |
+---------------------------------+
                |
                v
+---------------------------------+
| Data Rotation (Rotated Directory)|
+---------------------------------+
                |
                v
+---------------------------------+
| Data Analysis                    |
+---------------------------------+
```

This comprehensive documentation aims to provide new team members with a clear understanding of the project's data architecture, codebase, and workflows. For further details or specific queries, please refer to the source code and contact the project maintainers.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj17_Global_Eq_Study_codebase_*.tar.gz`
