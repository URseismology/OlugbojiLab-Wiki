> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# ROOT Subsystem for Advanced Seismic Wave Propagation Simulations

## 1. Project Overview

The `ROOT` subsystem is a comprehensive suite designed to support advanced seismic wave propagation simulations, spectral analysis, and data processing for geophysical research and engineering applications. It integrates several key sub-modules that work together to provide a robust and scalable workflow from data preparation to simulation execution and detailed analysis.

### Key Features:
- **Seismic Wave Propagation Simulations**: Advanced modeling of seismic waves.
- **Spectral Analysis**: Robust tools for spectral analysis, including multitaper methods.
- **Data Processing**: Efficient handling of large datasets and parallel computing.
- **Machine Learning Models**: Integration of machine learning techniques for data analysis.
- **Visualization**: Tools for visualizing simulation results and analysis outcomes.

### Sub-Modules:
1. **multitaper_code**: MATLAB-based tools for spectral analysis.
2. **EP_bliu**: Large-scale seismic simulations and ambient noise analysis.
3. **epic_related**: Randomized force simulations and signal analysis.
4. **epic_production**: Data preparation, simulation execution, machine learning models, and visualization.
5. **for_Chris**: Experiment management and advanced FTAN analysis.
6. **epic_legacy/mgmt**: Automation of computational tasks on HPC clusters.

## 2. Data Architecture

### Directory Structure
The `ROOT` subsystem is organized into a hierarchical directory structure to manage different types of data and configurations efficiently. The main directories are:

- **Data Directories**:
  - `multitaper_code/data/`: Contains input data for spectral analysis.
  - `EP_bliu/data/`: Stores large-scale simulation inputs and outputs.
  - `epic_related/data/`: Houses randomized force simulation data.
  - `epic_production/experiments/`: Manages experiment configurations, outputs, and results.

- **Output Directories**:
  - `multitaper_code/output/`: Results of spectral analysis.
  - `EP_bliu/output/`: Simulation results and FTAN processing outputs.
  - `epic_related/output/`: Signal analysis and force simulation results.
  - `epic_production/experiments/experiment_*/outputs/`: Experiment-specific outputs.

- **Configuration Directories**:
  - `multitaper_code/configs/`: Configuration files for spectral analysis.
  - `EP_bliu/configs/`: Simulation parameters and settings.
  - `epic_related/configs/`: Force simulation configurations.
  - `epic_production/experiments/experiment_*/configs/`: Experiment-specific configurations.

### Data Patterns
The data in the `ROOT` subsystem follows specific patterns to ensure consistency and ease of processing:

- **Simulation Outputs**: Files are named using a consistent pattern, e.g., `WAVE_SIM_*_dist_*_rad_*-*_ang_*_*.txt`.
  - Example: `WAVE_SIM_100_dist_500_rad_600-700_ang_30_40.txt`

- **Spectral Analysis Results**: Output files are organized by analysis type and parameters.
  - Example: `multitaper_output_spectral_analysis_20231001.csv`

- **Configuration Files**: Configuration files use a clear naming convention to indicate their purpose and experiment.
  - Example: `CIA_10000_config.txt`

## 3. Code Reference

### Sub-Module Overview
Each sub-module contains specific scripts and tools that perform different tasks in the seismic analysis workflow.

#### multitaper_code
- **Scripts**:
  - `multitaper_analysis.m`: Main script for performing spectral analysis using multitaper methods.
  - `rayleigh_wave_modeling.m`: Script for modeling Rayleigh waves.
- **Functions**:
  - `compute_multitaper_spectrum()`: Computes the multitaper spectrum of a signal.
  - `plot_spectrum()`: Plots the computed spectrum.

#### EP_bliu
- **Scripts**:
  - `run_large_scale_simulation.sh`: Shell script for running large-scale seismic simulations.
  - `ambient_noise_analysis.m`: MATLAB script for ambient noise analysis.
  - `ftan_processing.py`: Python script for FTAN processing.
- **Functions**:
  - `simulate_seismic_waves()`: Simulates seismic wave propagation.
  - `analyze_ambient_noise()`: Analyzes ambient noise data.

#### epic_related
- **Scripts**:
  - `randomized_force_simulation.m`: MATLAB script for randomized force simulations.
  - `signal_analysis.py`: Python script for advanced signal analysis.
- **Functions**:
  - `generate_random_forces()`: Generates random forces for simulation.
  - `analyze_signal()`: Analyzes signals using various techniques.

#### epic_production
- **Scripts**:
  - `prepare_data.sh`: Shell script for preparing data for experiments.
  - `run_experiment.py`: Python script for running experiments.
  - `visualize_results.py`: Python script for visualizing results.
- **Functions**:
  - `data_preparation()`: Prepares data for simulation.
  - `execute_simulation()`: Executes the simulation.
  - `train_ml_model()`: Trains machine learning models on simulation data.
  - `plot_results()`: Plots and visualizes the results.

#### for_Chris
- **Scripts**:
  - `experiment_management.sh`: Shell script for managing experiments.
  - `advanced_ftan_analysis.py`: Python script for advanced FTAN analysis.
- **Functions**:
  - `manage_experiment()`: Manages experiment setup and execution.
  - `perform_advanced_ftan()`: Performs advanced FTAN analysis.

#### epic_legacy/mgmt
- **Scripts**:
  - `setup_annular_regions.sh`: Shell script for setting up annular regions.
  - `submit_jobs_to_hpc.sh`: Shell script for submitting jobs to an HPC cluster.
- **Functions**:
  - `define_annular_regions()`: Defines annular regions for simulation.
  - `submit_job()`: Submits jobs to the HPC cluster.

## 4. Workflows

### Data Flow and Script Interactions
The `ROOT` subsystem follows a well-defined workflow that ensures data flows seamlessly through different stages of processing and analysis.

#### Data Preparation
1. **Data Collection**: Collect seismic data from various sources.
2. **Data Cleaning**: Clean and preprocess the data using scripts in `multitaper_code` and `EP_bliu`.
3. **Data Organization**: Organize the cleaned data into appropriate directories.

#### Simulation Execution
1. **Configuration Setup**: Set up configuration files for experiments in `epic_production/experiments/experiment_*/configs/`.
2. **Data Preparation**: Use `prepare_data.sh` to prepare data for simulation.
3. **Simulation Run**: Execute simulations using `run_large_scale_simulation.sh` and `run_experiment.py`.

#### Data Analysis
1. **Spectral Analysis**: Perform spectral analysis using `multitaper_analysis.m` and `rayleigh_wave_modeling.m`.
2. **Signal Analysis**: Analyze signals using `signal_analysis.py` in `epic_related`.
3. **FTAN Processing**: Process FTAN data using `ftan_processing.py` in `EP_bliu`.

#### Machine Learning
1. **Model Training**: Train machine learning models on simulation data using `train_ml_model()` in `epic_production`.
2. **Model Evaluation**: Evaluate the trained models and refine as necessary.

#### Visualization
1. **Result Plotting**: Visualize results using `plot_spectrum()`, `plot_results()`, and `visualize_results.py`.

### Example Workflow: Seismic Simulation and Analysis
1. **Data Collection**:
   - Collect seismic data from sensors.
   - Store the data in `EP_bliu/data/`.

2. **Data Cleaning**:
   - Clean the data using `clean_data.m` in `multitaper_code`.
   - Save the cleaned data in `EP_bliu/data/cleaned/`.

3. **Configuration Setup**:
   - Define simulation parameters in `epic_production/experiments/experiment_17/configs/CIA_0092/CIA_0092.txt`.

4. **Data Preparation**:
   - Run `prepare_data.sh` to prepare the data for simulation.
   - Data is organized in `epic_production/experiments/experiment_17/inputs/`.

5. **Simulation Execution**:
   - Execute simulations using `run_large_scale_simulation.sh`.
   - Simulation results are saved in `epic_production/experiments/experiment_17/outputs/`.

6. **Spectral Analysis**:
   - Perform spectral analysis on the simulation results using `multitaper_analysis.m`.
   - Save the results in `multitaper_code/output/`.

7. **Signal Analysis**:
   - Analyze signals using `signal_analysis.py` in `epic_related`.
   - Save the analyzed data in `epic_related/output/`.

8. **FTAN Processing**:
   - Process FTAN data using `ftan_processing.py` in `EP_bliu`.
   - Save the processed data in `EP_bliu/output/`.

9. **Machine Learning**:
   - Train machine learning models on the simulation data using `train_ml_model()` in `epic_production`.
   - Evaluate and refine the models.

10. **Visualization**:
    - Visualize the results using `plot_spectrum()`, `plot_results()`, and `visualize_results.py`.
    - Save the visualizations in `epic_production/experiments/experiment_17/visualizations/`.

By following this structured workflow, the `ROOT` subsystem ensures a seamless and efficient process from data collection to result visualization.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj_Wavenet_codebase_*.tar.gz`
