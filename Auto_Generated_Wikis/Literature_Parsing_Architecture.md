# Literature Parsing Architecture

To manage and analyze the lab's massive PDF library without losing structural fidelity of older scanned documents, we developed a local Deep-Learning OCR pipeline on the `inferencelocal` GPU node.

## 1. The Architecture

The entire pipeline is codified in `AIBot_PaperCrafter.py`. It runs iteratively to safely ingest and analyze PDFs directly from the Synology NAS. 

### Secure Ingestion & Deduplication
The pipeline securely mounts `repovibranium` via SFTP to search for new `.pdf` files in the `Paperpile` and `All Papers` directories. Before processing, it generates a SHA-256 hash of the remote file path and checks the local SQLite Database. If the paper has already been processed, it is instantly skipped, allowing the daemon to run continuously and recover from crashes seamlessly.

### Deep-Learning OCR (`marker-pdf`)
Instead of standard text parsers (like PyMuPDF) which completely fail on historical scanned documents (e.g., Gabor 1946), the pipeline utilizes `marker-pdf` v0.3.10. This runs native PyTorch models directly on the RTX 3090 GPUs to visually parse and transcribe scanned images into perfect Markdown format.

### Metadata Enrichment & API Lookup
The raw Markdown is scanned using regex to extract a clean DOI (e.g., `10.1073/pnas.2013806117`). If a DOI exists, the pipeline queries the `OpenAlex API` to retrieve clean, standardized metadata including:
* **Title**
* **Authors**
* **Journal Name**
* **Publication Year**
* **Citation Count**

### AI Synthesis (`Qwen2.5-72B`)
To prevent Out Of Memory (OOM) errors during AI synthesis, the raw text is aggressively truncated to 100,000 characters (approx. 25,000 tokens) which easily fits within Qwen2.5's context window on our local Ollama instance. The local LLM is then prompted to read the text and synthesize a strict 3-sentence summary covering the paper's core hypothesis, methodology, and conclusion.

## 2. Output & Artifact Generation

The pipeline generates three critical outputs for human consumption and downstream AI Agents:

1. **Master SQLite & JSONL Database:** The metadata and AI summaries are centralized into `Master_PaperCrafter_Index.jsonl` and a SQLite `.db` for bulk queries.
2. **Individual JSONL Summaries:** Standalone `.jsonl` files are created for every single paper in the `jsonl_summaries/` directory, allowing Cloud LLMs to easily cross-reference individual papers.
3. **Raw Markdown Extraction:** The full-text markdown output generated by the deep-learning OCR is preserved in the `full_texts/` directory.

## 3. Operations

To launch the background daemon and begin processing the library:

```bash
# SSH into the local GPU node
ssh urseismoadmin@128.151.53.34

# Launch the pipeline via nohup in the background
nohup /home/urseismoadmin/papercrafter_env_312/bin/python /home/urseismoadmin/AIBot_PaperCrafter.py > /home/urseismoadmin/AIBot_PaperCrafter.log 2>&1 &
```
