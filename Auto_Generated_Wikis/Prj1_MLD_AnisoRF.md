![AI Generated](https://img.shields.io/badge/AI-Generated-blue)

> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# African Lithosphere and Marine Left Dorsal Anisotropy Research Framework (MLD-AnisoRF) Documentation

## 1. Project Overview

The African Lithosphere and Marine Left Dorsal Anisotropy Research Framework (MLD-AnisoRF) is a comprehensive system designed for the analysis of seismic data, particularly focusing on back azimuth and distance patterns to understand seismic activity. The primary script, `plotBazAndDist_all.m`, organizes seismic events into back azimuth and distance buckets, generates polar plots for event density visualization, and scatter plots for epicentral distance versus ray parameter. This tool is essential for seismological research and supports disaster preparedness.

## 2. Data Architecture

### Root Directory Structure
The root directory contains several sub-modules organized to facilitate data storage, processing, and analysis:

- **1_Docs**: Documentation files.
- **2_Data**: Raw and processed seismic data.
- **3_Src**: Source code for data processing and visualization.
- **4_Bin**: Additional tools and utilities.

### Data Patterns
The `2_Data` directory is structured to store seismic data in a hierarchical manner:

- **SAC (Standard for the Exchange of Earthquake Data)**: This sub-directory contains SAC files organized by network, station, and channel. Each station directory includes various seismic channels (e.g., BHE, BHN, BHZ) and their respective SAC files.

#### Example Directory Structure
```
2_Data/
├── SAC/
│   ├── NM/  # Network NM
│   │   ├── PLAL/  # Station PLAL
│   │   │   ├── NM.PLAL.*..T (431 files, 25.10 MB)
│   │   │   ├── NM.PLAL.*.*.R (441 files, 52.75 MB)
│   │   │   └── alreadyHaveSAC.mat (1 files, 0.00 MB)
│   │   └── SIUC/  # Station SIUC
│   │       ├── NM.SIUC.*.*.BHZ.SAC (340 files, 40.64 MB)
│   │       └── alreadyHaveSAC.mat (1 files, 0.00 MB)
│   ├── IU/  # Network IU
│   │   ├── CCM/  # Station CCM
│   │   │   ├── IU.CCM.*..BHN.SAC (533 files, 31.99 MB)
│   │   │   └── alreadyHaveSAC.mat (1 files, 0.00 MB)
│   │   └── TUC/  # Station TUC
│   │       ├── IU.TUC.*.*.BH*.SAC (17874 files, 1746.30 MB)
│   │       └── alreadyHaveSAC.mat (1 files, 0.00 MB)
│   └── US/  # Network US
│       ├── EGMT/  # Station EGMT
│       │   ├── US.EGMT.*..BHN.SAC (636 files, 75.89 MB)
│       │   └── alreadyHaveSAC.mat (1 files, 0.00 MB)
│       └── JFWS/  # Station JFWS
│           ├── US.JFWS.*..BHZ.SAC (849 files, 101.26 MB)
│           └── alreadyHaveSAC.mat (1 files, 0.00 MB)
```

### Data Files
- **SAC Files**: These are binary data files containing seismic waveform data. Each file is named according to the network, station, location, channel, and time.
- **alreadyHaveSAC.mat**: A MATLAB mat-file indicating which SAC files have already been processed.

## 3. Code Reference

### Main Script: `plotBazAndDist_all.m`
The primary script for organizing and visualizing seismic events:

```matlab
function plotBazAndDist_all(dataPath)
    % Load data
    data = loadSeismicData(dataPath);

    % Organize data into back azimuth and distance buckets
    [baz, dist] = organizeDataByBazAndDist(data);

    % Generate polar plots for event density
    figure;
    plotPolar(baz, dist);
    title('Event Density by Back Azimuth and Distance');

    % Generate scatter plots for epicentral distance vs ray parameter
    figure;
    plotScatter(dist, data.rayParameter);
    xlabel('Epicentral Distance (km)');
    ylabel('Ray Parameter');
end
```

### Supporting Scripts in `3_Src`
- **`remote_startup.m`**: Sets up the MATLAB environment for remote processing.
  ```matlab
  function remote_startup()
      % Add paths to MATLAB's search path
      addpath(genpath(fullfile(pwd, '4_Bin')));
      addpath(genpath(fullfile(pwd, '2_Data')));
  end
  ```

- **`Single_Station_RF_Workflow.m`**: Workflow for processing receiver functions (RFs) at a single station.
  ```matlab
  function Single_Station_RF_Workflow(station)
      % Load data for the specified station
      data = loadStationData(station);

      % Process RFs
      rfData = processRF(data);

      % Save processed RFs
      saveRfData(rfData, station);
  end
  ```

- **`synRF.m`**: Generates synthetic receiver functions.
  ```matlab
  function synData = synRF(model)
      % Generate synthetic data based on the provided model
      synData = generateSyntheticData(model);
  end
  ```

- **`Transect.m`**: Performs transect analysis along a specified path.
  ```matlab
  function transectAnalysis(path, data)
      % Perform transect analysis
      results = performTransectAnalysis(path, data);

      % Visualize results
      plotTransect(results);
  end
  ```

### Utility Scripts in `4_Bin`
- **`mohoReverbTimes.m`**: Calculates Moho reverberation times.
  ```matlab
  function mohoTimes = mohoReverbTimes(data)
      % Calculate Moho reverberation times
      mohoTimes = calculateMohoReverberationTimes(data);
  end
  ```

- **`plotDepth.m`**: Plots depth profiles.
  ```matlab
  function plotDepth(data, depths)
      % Plot depth profiles
      figure;
      plot(depths, data);
      xlabel('Depth (km)');
      ylabel('Data');
  end
  ```

## 4. Workflows

### Data Flow Through Scripts

1. **Data Ingestion**
   - The `plotBazAndDist_all.m` script loads seismic data from the specified path using `loadSeismicData`.
   ```matlab
   function data = loadSeismicData(dataPath)
       % Load SAC files from the specified directory
       sacFiles = dir(fullfile(dataPath, '**', '*.SAC'));
       data = [];
       for i = 1:length(sacFiles)
           data(i) = readSACFile(fullfile(sacFiles(i).folder, sacFiles(i).name));
       end
   end
   ```

2. **Data Organization**
   - The loaded data is organized into back azimuth and distance buckets using `organizeDataByBazAndDist`.
   ```matlab
   function [baz, dist] = organizeDataByBazAndDist(data)
       % Extract back azimuth and epicentral distance from data
       baz = [data.baz];
       dist = [data.dist];
   end
   ```

3. **Visualization**
   - Polar plots for event density are generated using `plotPolar`.
   ```matlab
   function plotPolar(baz, dist)
       % Create a polar plot
       polarhistogram(baz * pi / 180, 'BinWidth', pi/18);
   end
   ```
   - Scatter plots for epicentral distance versus ray parameter are generated using `plotScatter`.
   ```matlab
   function plotScatter(dist, rayParameter)
       % Create a scatter plot
       scatter(dist, rayParameter);
   end
   ```

4. **Advanced Analysis**
   - The `Single_Station_RF_Workflow` script processes receiver functions for a single station.
   ```matlab
   function processRF(data)
       % Perform RF processing (e.g., deconvolution, filtering)
       rfData = performRFProcessing(data);
       return rfData;
   end
   ```

5. **Synthetic Data Generation**
   - The `synRF` script generates synthetic receiver functions based on a specified model.
   ```matlab
   function synData = generateSyntheticData(model)
       % Generate synthetic data based on the provided model
       synData = simulateSeismicModel(model);
   end
   ```

6. **Transect Analysis**
   - The `Transect` script performs transect analysis along a specified path.
   ```matlab
   function results = performTransectAnalysis(path, data)
       % Perform transect analysis
       results = analyzeTransect(path, data);
   end
   ```

### Example Workflow

1. **Load Data**
   - Run `plotBazAndDist_all('2_Data/SAC/NM/PLAL/')` to load and process seismic data for station PLAL in network NM.

2. **Organize Data**
   - The script organizes the data into back azimuth and distance buckets.

3. **Generate Plots**
   - Polar plots and scatter plots are generated to visualize event density and epicentral distance versus ray parameter.

4. **Advanced Analysis**
   - Run `Single_Station_RF_Workflow('PLAL')` to process receiver functions for station PLAL.

5. **Synthetic Data Generation**
   - Run `synRF(model)` to generate synthetic receiver functions based on a specified seismic model.

6. **Transect Analysis**
   - Run `Transect(path, data)` to perform transect analysis along a specified path.

This structured approach ensures that the MLD-AnisoRF system is robust, modular, and easy to extend for future research needs.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj1_MLD_AnisoRF_codebase_*.tar.gz`
