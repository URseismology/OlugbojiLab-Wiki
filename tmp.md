> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# Project Overview

## Introduction
This project is a comprehensive suite of MATLAB and Python scripts designed for dynamic system simulations and data analysis, with a focus on matrix manipulations, inverted pendulum dynamics, and seismic instrument orientation determination using Rayleigh waves. The project leverages high-performance computing (HPC) environments managed by Slurm, utilizing Message Passing Interface (MPI) for parallel processing to handle large-scale computations efficiently.

## Key Features
- **Dynamic System Simulations**: Scripts for simulating various dynamic systems, including inverted pendulums.
- **Matrix Manipulations**: Tools for performing complex matrix operations and linear algebra calculations.
- **Seismic Data Analysis**: Functions for analyzing seismic data, particularly for determining instrument orientation using Rayleigh waves.
- **HPC Integration**: Utilizes Slurm for job management and MPI for parallel processing to ensure efficient resource utilization.

## Sub-Modules
The project is organized into several sub-modules, each designed to handle specific tasks and MATLAB versions:
- **lmoser**
- **tolugboj**
- **jpippin**
- **bliu17**
- **tbrink**
- **zzhang90**

Each sub-module follows a consistent structure, centered around the `communicatingJobWrapper.sh` script, which manages job submission, resource allocation, and error handling.

# Data Architecture

## Directory Structure
The project is structured as follows:

```
project_root/
├── lmoser/
│   ├── Job1/
│   ├── Job2/
│   └── ...
├── tolugboj/
│   ├── Job15/
│   ├── Job16/
│   ├── R2018a/
│   │   ├── Job7/
│   │   ├── Job8/
│   │   └── ...
│   ├── ...
├── jpippin/
│   ├── ...
├── bliu17/
│   ├── ...
├── tbrink/
│   ├── ...
├── zzhang90/
│   ├── ...
└── scripts/
    ├── communicatingJobWrapper.sh
    ├── matlab_init.m
    └── ...
```

### Data Patterns
- **Job Directories**: Each job directory (e.g., `Job15`) contains files specific to a particular MATLAB job, including input data (`Task*.in.mat`), output data (`Task*.out.mat`), logs (`Job*.log`, `Task*.java.log.*`), and state files (`Task*.state.mat`).
- **Common Files**: Shared across jobs within a sub-module, such as `matlab_metadata.mat` and `communicatingJobWrapper.sh`.
- **Version-Specific Sub-Modules**: Some sub-modules (e.g., `R2018a`) are specific to particular MATLAB versions and may contain additional configuration files or scripts.

### Example Data Flow
1. **Input Data**: `Task*.in.mat` contains the initial data for the job.
2. **Logs**: `Job*.log` and `Task*.java.log.*` capture runtime information and errors.
3. **Output Data**: `Task*.out.mat` stores the results of the computation.
4. **State Files**: `Task*.state.mat` keeps track of the job's state during execution.

# Code Reference

## Key Scripts
### `communicatingJobWrapper.sh`
This script is crucial for managing MPI and Slurm interactions. It sets up environment variables, initializes SMPD or MPI processes, and handles job submission and resource allocation.

#### Usage
```sh
./communicatingJobWrapper.sh <job_directory>
```

#### Example Content
```sh
#!/bin/bash

# Set environment variables
export MATLABPATH=/path/to/matlab/toolboxes

# Initialize MPI
mpirun -np $SLURM_NTASKS --oversubscribe matlab -nodisplay -nosplash -r "communicatingJobDriver('$1')"

# Handle exit codes
if [ $? -ne 0 ]; then
    echo "Job failed with exit code $?"
    exit 1
fi
```

### `matlab_init.m`
This script initializes the MATLAB environment and sets up necessary paths and configurations.

#### Example Content
```matlab
function matlab_init()
    % Add toolboxes to path
    addpath('/path/to/matlab/toolboxes');
    
    % Initialize global variables
    global jobDir;
    jobDir = 'path/to/job_directory';
end
```

### `communicatingJobDriver.m`
This script is the entry point for each MPI process. It loads the input data, performs the necessary computations, and saves the output.

#### Example Content
```matlab
function communicatingJobDriver(jobDir)
    % Initialize MATLAB environment
    matlab_init();
    
    % Load input data
    load(fullfile(jobDir, 'Task*.in.mat'));
    
    % Perform computations
    result = performComputation(inputData);
    
    % Save output data
    save(fullfile(jobDir, 'Task*.out.mat'), 'result');
end
```

### `performComputation.m`
This script contains the core logic for performing the required computations.

#### Example Content
```matlab
function result = performComputation(inputData)
    % Perform matrix manipulations or dynamic system simulations
    result = someComplexFunction(inputData);
end
```

# Workflows

## Data Flow Through Scripts
1. **Job Submission**:
   - The user submits a job to the Slurm scheduler using `sbatch`.
   ```sh
   sbatch submit_job.sh <job_directory>
   ```

2. **Environment Setup**:
   - The `communicatingJobWrapper.sh` script is executed, setting up the necessary environment variables and initializing MPI.

3. **Data Loading**:
   - The `matlab_init.m` script initializes the MATLAB environment and sets up paths.
   - The `communicatingJobDriver.m` script loads the input data from `Task*.in.mat`.

4. **Computation**:
   - The `performComputation.m` script performs the required computations using the loaded data.

5. **Data Saving**:
   - The results of the computation are saved to `Task*.out.mat`.
   - State information is saved to `Task*.state.mat`.

6. **Error Handling and Cleanup**:
   - The `communicatingJobWrapper.sh` script handles any errors and ensures clean job termination.
   - Resource allocation is released, and logs are generated.

### Example Workflow
1. **Submit Job**:
   ```sh
   sbatch submit_job.sh /path/to/tolugboj/Job15
   ```

2. **Initialize Environment**:
   - `communicatingJobWrapper.sh` sets up environment variables and initializes MPI.
   - `matlab_init.m` adds necessary paths and initializes global variables.

3. **Load Data**:
   - `communicatingJobDriver.m` loads input data from `/path/to/tolugboj/Job15/Task*.in.mat`.

4. **Perform Computation**:
   - `performComputation.m` processes the input data and generates results.

5. **Save Results**:
   - Results are saved to `/path/to/tolugboj/Job15/Task*.out.mat`.
   - State information is saved to `/path/to/tolugboj/Job15/Task*.state.mat`.

6. **Cleanup**:
   - `communicatingJobWrapper.sh` ensures clean job termination and resource release.
   - Logs are generated in `/path/to/tolugboj/Job15/Job*.log`.

By following this structured workflow, the project ensures efficient and reliable execution of complex MATLAB jobs on HPC clusters.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `tmp_codebase_*.tar.gz`
