> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
### Seismic Data Processing Framework: Comprehensive Documentation

#### 1. Project Overview

This project is a comprehensive MATLAB-based framework designed for advanced seismic data processing, analysis, and visualization. The primary goal of this framework is to facilitate seamless and efficient handling of large seismic datasets, enabling researchers to perform complex analyses such as travel time calculations, receiver function (RF) analysis, H-k stacking, and geospatial visualization.

The project is structured into several subsystems, each tailored for specific tasks. These subsystems include:

- **Data Preprocessing**: Handling raw data, converting formats, and preparing datasets for analysis.
- **Signal Processing**: Advanced signal processing techniques such as FISTA (Fast Iterative Shrinkage-Thresholding Algorithm) and MTC (Multi-Taper Coherence).
- **Receiver Function Analysis (RF)**: Analyzing receiver functions to study the Earth's crustal structure.
- **H-k Stacking**: Performing H-k stacking to determine hypocentral parameters.
- **Geospatial Visualization**: Visualizing seismic data on maps for better interpretation.

#### 2. Data Architecture

The data architecture is designed to handle large volumes of seismic data efficiently. The data is organized into a hierarchical structure, with each subsystem containing its own data directory. Here's an overview of the data patterns:

- **Raw Data**: Raw seismic data in various formats (e.g., SAC, MSEED) are stored in dedicated directories within each subsystem.
- **Processed Data**: Intermediate and final processed datasets are saved in specific subdirectories, often with descriptive names to indicate the processing steps.
- **Metadata**: Metadata files such as `inwave.txt` and `allComp.mat` provide essential information about the data, including event details and component orientations.

**Example Data Structure:**

```
Day_2/
├── 2_Data/
│   ├── SAC/
│   │   ├── IU/
│   │   │   ├── ANTO/
│   │   │   │   ├── IU.ANTO.*.*.BHZ.SAC
│   │   │   │   ├── IU.ANTO.*.*.BHN.SAC
│   │   │   │   ├── IU.ANTO.*..BHE.SAC
│   │   │   │   ├── IU.ANTO_inwave.txt
│   │   │   │   └── IU_ANTO_allComp.mat
│   │   ├── FURI/
│   │   │   ├── IU.FURI.*.*.BHZ.SAC
│   │   │   ├── IU.FURI.*..BHE.SAC
│   │   │   ├── IU_FURI_inwave.txt
│   │   │   └── IU_FURI_allComp.mat
├── 3_Src/
│   ├── RFImager/
│   │   ├── 1_Functions/
│   │   │   ├── TauP-2.0/
│   │   │   │   ├── src/main/java/edu/sc/seis/TauP/
│   │   │   │   │   ├── VelocityModel.java
│   │   │   │   │   ├── SeismicPhase.java
│   │   │   │   │   └── TauP_Time.java
│   │   │   │   └── src/main/resources/edu/sc/seis/TauP/StdModels/
│   │   │   │       ├── herrin.nd
│   │   │   │       ├── ak135.tvel
│   │   │   │       └── prem.nd
```

#### 3. Code Reference

The codebase is modular, with each subsystem containing specific scripts and functions to handle its tasks. Here's a detailed breakdown of key components:

- **Core Scripts**: These are the main scripts that orchestrate the workflow for each subsystem.
- **Function Libraries**: These contain reusable functions for data processing, analysis, and visualization.
- **Data Handling Modules**: These modules manage the loading, preprocessing, and saving of seismic data.

**Example Core Script: `runEntireWrkflow.m` in `CRISP-RF-main`**

```matlab
% runEntireWrkflow.m
function runEntireWrkflow()
    % Load configuration
    config = loadConfig();
    
    % Step 1: Data Preprocessing
    dataDir = fullfile(config.dataDir, '2_Data/SAC');
    processedDataDir = fullfile(config.dataDir, '3_ProcessedData');
    preprocessData(dataDir, processedDataDir);
    
    % Step 2: Receiver Function Analysis
    rfDataDir = fullfile(processedDataDir, 'RF');
    analyzeReceiverFunctions(processedDataDir, rfDataDir);
    
    % Step 3: H-k Stacking
    hkStackingResults = performHkStacking(rfDataDir);
    
    % Step 4: Visualization
    visualizeResults(hkStackingResults, config.outputDir);
end
```

**Example Function Library: `Functions_FADER` in `FADER`**

```matlab
% Functions_FADER.m
function [reverberations] = detectReverberations(data)
    % Detect reverberations using FISTA algorithm
    parameters = getFistaParameters();
    reverberations = fistaAlgorithm(data, parameters);
end

function [results] = analyzeReceiverFunctions(dataDir, outputDir)
    % Analyze receiver functions and save results
    data = loadSeismicData(dataDir);
    rfResults = calculateReceiverFunctions(data);
    saveResults(rfResults, outputDir);
end
```

#### 4. Workflows

The workflows in this project are designed to be modular and flexible, allowing researchers to perform specific tasks or run the entire pipeline from start to finish. Here's a high-level overview of the data flow through the scripts:

1. **Data Preprocessing**:
   - Raw seismic data is loaded from the `2_Data/SAC` directory.
   - Data preprocessing steps such as filtering, detrending, and normalization are applied.
   - Processed data is saved to the `3_ProcessedData` directory.

2. **Receiver Function Analysis (RF)**:
   - The processed data is read from the `3_ProcessedData` directory.
   - Receiver functions are calculated using methods like deconvolution.
   - RF results are saved to the `4_RFResults` directory.

3. **H-k Stacking**:
   - RF results are loaded from the `4_RFResults` directory.
   - H-k stacking is performed to determine hypocentral parameters.
   - Stacking results are saved to the `5_HkStacking` directory.

4. **Geospatial Visualization**:
   - H-k stacking results are read from the `5_HkStacking` directory.
   - Results are visualized on geospatial maps using tools like `m_map`.
   - Visualizations are saved to the `6_Visualizations` directory.

**Example Workflow:**

1. **Data Preprocessing**:
   ```matlab
   % Load and preprocess data
   dataDir = 'Day_2/2_Data/SAC/IU/ANTO';
   processedDataDir = 'Day_2/3_ProcessedData/IU/ANTO';
   preprocessData(dataDir, processedDataDir);
   ```

2. **Receiver Function Analysis**:
   ```matlab
   % Analyze receiver functions
   rfDataDir = 'Day_2/4_RFResults/IU/ANTO';
   analyzeReceiverFunctions(processedDataDir, rfDataDir);
   ```

3. **H-k Stacking**:
   ```matlab
   % Perform H-k stacking
   hkStackingResults = performHkStacking(rfDataDir);
   saveResults(hkStackingResults, 'Day_2/5_HkStacking/IU/ANTO');
   ```

4. **Geospatial Visualization**:
   ```matlab
   % Visualize results
   visualizeResults('Day_2/5_HkStacking/IU/ANTO', 'Day_2/6_Visualizations/IU/ANTO');
   ```

By following these workflows, researchers can efficiently process and analyze seismic data, from raw data ingestion to final visualization. The modular design ensures that each step is well-defined and can be customized as needed for specific research objectives.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj_Stephen_NLCrust_codebase_*.tar.gz`
