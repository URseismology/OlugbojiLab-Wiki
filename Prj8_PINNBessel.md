> [!WARNING]
> **AI Hallucination Disclaimer**
> This Wiki was automatically generated by Large Language Models (`DeepSeek-Coder-V2` & `Qwen2.5-72B`) to accelerate onboarding. While structurally accurate, it may occasionally hallucinate specific function calls, variable names, or execution commands (e.g., pseudo-MATLAB commands). Always cross-reference the suggested workflows with the actual source code.
> 
> *For more details on how this was generated, see [0_AI_Generation_Process](0_AI_Generation_Process.md)*

---
# Project Overview

The `ROOT` subsystem is a comprehensive suite designed for advanced geophysical data analysis, particularly focusing on seismic exploration, signal processing, and network modeling. It integrates MATLAB and Python environments to handle tasks ranging from creating custom ASCII files based on the Litho1.0 model to analyzing Bessel function envelopes, comparing synthetic and theoretical phase velocities, processing NCF signals, and modifying seismic data amplitudes. The system also includes neural network applications for estimating phase velocities.

## Key Features
- **Data Generation**: Custom ASCII files generation using the Litho1.0 model.
- **Signal Processing**: Advanced signal processing techniques including Bessel function analysis and amplitude modification.
- **Neural Networks**: Estimation of phase velocities using deep learning models.
- **Ambient Noise Processing**: Framework for ambient noise cross-correlation functions (CCFs) and dispersion curve calculations.
- **HPC Integration**: Automated job submission and output file management in an HPC environment.

# Data Architecture

### Data Patterns
The data architecture is organized into three main modules: `spac`, `SynStack`, and `OtherData`. Each module serves a specific purpose and follows a structured pattern for storing and processing data.

#### 1. `spac` Module
- **Data Storage**: 
  - **Input Data**: Seismic datasets, synthetic models, and configuration files.
  - **Output Data**: Cross-correlation functions (CCFs), dispersion curves, and analysis results.
- **File Structure**:
  - **Scripts**: MATLAB scripts for data processing and analysis.
  - **Functions**: Reusable MATLAB functions for specific tasks.
  - **Results**: Subdirectories for storing output files and figures.

#### 2. `SynStack` Module
- **Data Storage**:
  - **Input Data**: Synthetic seismic models, noise configurations, and job parameters.
  - **Output Data**: Processed synthetic data, dispersion curves, and analysis results.
- **File Structure**:
  - **Job Scripts**: HPC job submission scripts.
  - **Results**: Subdirectories for storing output files and figures from HPC jobs.

#### 3. `OtherData` Module
- **Data Storage**:
  - **Input Data**: Real seismic datasets, synthetic models, and experiment configurations.
  - **Output Data**: Analysis results, dispersion curves, and cross-correlation functions.
- **File Structure**:
  - **Scripts**: MATLAB and Python scripts for data analysis.
  - **Results**: Subdirectories for storing output files and figures.

### Example File Paths
- `spac/results/raleigh_orig/synthetics_100/Litho_5/figs/window1hr/ccf.ps`
- `SynStack/SyntheticStacks/SyntheticData_100/Litho_5/stacked_data.mat`
- `OtherData/experiment_16/data/synthetic_data.mat`

# Code Reference

### 1. `spac` Module
The `spac` module is the core of the system, providing a framework for ambient noise processing and cross-correlation function (CCF) analysis.

#### Scripts
- **`ccf.m`**: Computes cross-correlation functions from seismic data.
- **`dispersion_curve.m`**: Calculates dispersion curves from CCFs.
- **`plot_results.m`**: Generates plots for visualizing results.

#### Functions
- **`getLitho1Ds.m`**: Creates custom ASCII files based on the Litho1.0 model.
- **`getBesselEnve.m`**: Analyzes Bessel function envelopes.
- **`ApplyAmpChge.m`**: Modifies seismic data amplitudes.

#### Example Usage
```matlab
% Generate custom ASCII file
lithoFile = getLitho1Ds('Litho_5');

% Compute cross-correlation functions
ccfData = ccf(lithoFile, 'window', 1);

% Calculate dispersion curve
dispCurve = dispersion_curve(ccfData);

% Plot results
plot_results(dispCurve);
```

### 2. `SynStack` Module
The `SynStack` module automates the process of generating and processing synthetic seismic data in an HPC environment.

#### Scripts
- **`submit_jobs.sh`**: Submits jobs to the HPC cluster.
- **`process_output.py`**: Processes output files from HPC jobs.

#### Functions
- **`generate_synthetic_data.m`**: Generates synthetic seismic data.
- **`stack_data.m`**: Stacks multiple synthetic datasets.

#### Example Usage
```bash
# Submit HPC job
./submit_jobs.sh

# Process output files
python process_output.py --input_dir=SyntheticData_100 --output_dir=ProcessedData
```

### 3. `OtherData` Module
The `OtherData` module facilitates the analysis and visualization of both real and synthetic seismic datasets.

#### Scripts
- **`plot_syn_data.m`**: Plots synthetic data.
- **`compare_disp_curves.m`**: Compares dispersion curves from different datasets.

#### Functions
- **`load_seismic_data.m`**: Loads seismic data from files.
- **`compute_ncf.m`**: Computes noise correlation functions (NCFs).

#### Example Usage
```matlab
% Load real and synthetic data
realData = load_seismic_data('real_data.mat');
synData = load_seismic_data('synthetic_data.mat');

% Compute NCFs
ncfReal = compute_ncf(realData);
ncfSyn = compute_ncf(synData);

% Plot results
plot_syn_data(ncfReal, ncfSyn);
```

# Workflows

### Data Flow Through Scripts

1. **Data Generation**
   - **`getLitho1Ds.m`**: Generates custom ASCII files based on the Litho1.0 model.
   - **Output**: Custom ASCII file (`lithoFile`).

2. **Seismic Signal Processing**
   - **`getBesselEnve.m`**: Analyzes Bessel function envelopes from the generated data.
   - **Output**: Envelope analysis results.

3. **Cross-Correlation Function (CCF) Analysis**
   - **`ccf.m`**: Computes cross-correlation functions from seismic data.
   - **Input**: Seismic data (`lithoFile`).
   - **Output**: Cross-correlation functions (`ccfData`).

4. **Dispersion Curve Calculation**
   - **`dispersion_curve.m`**: Calculates dispersion curves from CCFs.
   - **Input**: Cross-correlation functions (`ccfData`).
   - **Output**: Dispersion curve data (`dispCurve`).

5. **Result Visualization**
   - **`plot_results.m`**: Generates plots for visualizing results.
   - **Input**: Dispersion curve data (`dispCurve`).
   - **Output**: Plots and figures.

### Synthetic Data Workflow

1. **Generate Synthetic Data**
   - **`generate_synthetic_data.m`**: Generates synthetic seismic data based on specified parameters.
   - **Output**: Synthetic data files.

2. **Submit HPC Jobs**
   - **`submit_jobs.sh`**: Submits jobs to the HPC cluster for processing synthetic data.
   - **Output**: Job submission logs.

3. **Process Output Files**
   - **`process_output.py`**: Processes output files from HPC jobs, stacking and analyzing the results.
   - **Input**: Output files from HPC jobs.
   - **Output**: Processed synthetic data and analysis results.

### Real Data Analysis Workflow

1. **Load Seismic Data**
   - **`load_seismic_data.m`**: Loads real seismic data from files.
   - **Output**: Real seismic data (`realData`).

2. **Compute Noise Correlation Functions (NCFs)**
   - **`compute_ncf.m`**: Computes noise correlation functions from the loaded data.
   - **Input**: Seismic data (`realData`).
   - **Output**: NCFs (`ncfReal`).

3. **Compare Dispersion Curves**
   - **`compare_disp_curves.m`**: Compares dispersion curves from real and synthetic datasets.
   - **Input**: Real and synthetic NCFs (`ncfReal`, `ncfSyn`).
   - **Output**: Comparison results.

4. **Plot Results**
   - **`plot_syn_data.m`**: Generates plots for visualizing the comparison results.
   - **Input**: Comparison results.
   - **Output**: Plots and figures.

By following these workflows, the `ROOT` subsystem ensures a structured and efficient process for geophysical data analysis, from data generation to result visualization.
---
## 📦 Download Codebase
The complete physical codebase for this project is hosted securely on the Lab NAS.
* **NAS URL:** [https://repovibranium.synology.me:5001/](https://repovibranium.synology.me:5001/)
* **File Station Path:** `/ADAMA-Shared/GodModeData/CodeBaseFull/`
* **Search For:** `Prj8_PINNBessel_codebase_*.tar.gz`
